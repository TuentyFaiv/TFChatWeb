import { FC } from "react";

import {
  ChatInfo as ChatInfoContainer
} from "@stylesComponents/ChatInfo";

const ChatInfo: FC = () => {
  return (
    <ChatInfoContainer>
      <p>Chat info</p>
    </ChatInfoContainer>
  );
}

export default ChatInfo;